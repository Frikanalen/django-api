apiVersion: batch/v1
kind: CronJob
metadata:
  name: fill-with-jukebox
spec:
  schedule: "00 23 * * 6"
  jobTemplate:
    spec:
      template:
        metadata:
          labels:
            app: django-api
        spec:
          restartPolicy: OnFailure
          containers:
            - name: django-api
              image: {{ .Values.image.django }}
              args:
                - ./manage.py
                - plan_iso_week
                - -v
                - '2'
              env:
                {{- include "django.env" . | nindent 16 }}
